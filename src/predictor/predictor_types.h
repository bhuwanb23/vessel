#pragma once

// =============================================================================
// Predictor Types - now just includes the shared types header
// =============================================================================
// All types are defined in include/types.h for Step 4 unified binary.
// This header exists for backward compatibility with Step 3 code.
// =============================================================================

#include "../../include/types.h"
